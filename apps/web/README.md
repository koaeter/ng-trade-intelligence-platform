# NG Trade Intelligence Web

The web application will be implemented as the frontend for the platform API.

Pass 15 establishes the application boundary only; the first functional UI arrives with the initial vertical slice.