# Docker Development

Docker assets for local and deployment environments will be added as the executable platform grows.

The root `docker-compose.yml` currently provides the PostgreSQL development service.