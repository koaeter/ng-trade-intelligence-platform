Pass 80 links the authority endpoint catalogue to the source lifecycle. A Source now optionally records endpoint_id, preserving Authority -> Endpoint -> Source provenance. The registration service validates that the endpoint exists and is active, rejects mismatched source URLs, and retains the endpoint identifier. It does not acquire content or publish regulatory knowledge; those remain later lifecycle steps.