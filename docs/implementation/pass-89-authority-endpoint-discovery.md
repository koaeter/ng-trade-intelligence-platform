# Pass 89 — Authority endpoint discovery API

The API now exposes active authority endpoints by authority ID. This gives clients a controlled discovery path for source registration instead of requiring endpoint URLs to be entered manually.
