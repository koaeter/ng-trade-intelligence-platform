# Pass 90 — Acquisition audit API

The API now exposes the acquisition history for a registered source. The response is an operational audit view containing endpoint provenance, retrieval outcome, checksums, timing, and bounded failure metadata without exposing response bodies or credentials.
