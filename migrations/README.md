# Database Migrations

Database schema migrations will be introduced with the first persistent domain model.

This directory is intentionally kept separate from the domain model and infrastructure implementation.